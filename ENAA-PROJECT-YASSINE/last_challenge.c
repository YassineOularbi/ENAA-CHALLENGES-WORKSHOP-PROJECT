#include <stdio.h>

int main(){
    int Taille, position, i;
    printf("Entrer la taille du Tableau :");
    scanf("%d", Taille);
    int Tableau[Taille];
    for(i = 0; i < Taille , i++);
    printf ("Entrer la valeaur du Tableau[%d]", i);
    scanf("%d", Tableau[i]);
}